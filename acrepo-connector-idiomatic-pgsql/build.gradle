group = 'cool.pandora'
description = 'ID Mapping Postgresql Connector'

artifacts {
    archives (file('build/cfg/main/cool.pandora.connector.idiomatic.pgsql.cfg')) {
        classifier 'configuration'
        type 'cfg'
    }
}
