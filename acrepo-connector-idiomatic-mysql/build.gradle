group = 'cool.pandora'
description = 'ID Mapping MySQL Connector'

artifacts {
    archives (file('build/cfg/main/cool.pandora.connector.idiomatic.mysql.cfg')) {
        classifier 'configuration'
        type 'cfg'
    }
}
